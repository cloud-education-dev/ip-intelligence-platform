import { evaluateAcl, evaluateSecurityGroup, FirewallRule } from '../../domain/security/firewall';
const sg:FirewallRule[]=[{id:'https',priority:100,direction:'inbound',action:'allow',protocol:'tcp',portFrom:443,portTo:443,cidr:'0.0.0.0/0'}];
const nacl:FirewallRule[]=[{id:'denybad',priority:90,direction:'inbound',action:'deny',protocol:'all',cidr:'203.0.113.0/24'},{id:'allowweb',priority:100,direction:'inbound',action:'allow',protocol:'tcp',portFrom:443,portTo:443,cidr:'0.0.0.0/0'}];
export function SecuritySimulator(){ const packet={direction:'inbound' as const,protocol:'tcp' as const,port:443,peerIp:'203.0.113.10'}; const sgRes=evaluateSecurityGroup(sg,packet); const aclRes=evaluateAcl(nacl,packet); return <section className="card p-5"><h2 className="text-2xl font-black">Modules 12 & 13 — Security Group and NACL Simulator</h2><p className="text-slate-400">Compare AWS stateful security groups with stateless ordered network ACLs.</p><div className="mt-4 grid gap-4 md:grid-cols-2"><Result title="Security Group" res={sgRes}/><Result title="Network ACL" res={aclRes}/></div><p className="mt-4 rounded-xl bg-white/10 p-3 text-sm">Packet: inbound TCP/443 from 203.0.113.10. SG allows because explicit allow matched. NACL denies because lower-number deny rule wins before the allow rule.</p></section> }
function Result({title,res}:{title:string;res:{allowed:boolean;explanation:string;matchedRule?:FirewallRule}}){return <div className={`rounded-2xl border p-4 ${res.allowed?'border-good/30 bg-good/10':'border-bad/30 bg-bad/10'}`}><h3 className="font-bold">{title}: {res.allowed?'ALLOW':'DENY'}</h3><p className="text-sm text-slate-300">{res.explanation}</p><pre className="mt-3 overflow-auto rounded-xl bg-black/30 p-3 text-xs">{JSON.stringify(res.matchedRule,null,2)}</pre></div>}
