import { useMemo, useState } from 'react';
import { lookupRoute, Route } from '../../domain/routing/routing';
const routes:Route[]=[{id:'local',destination:'10.20.0.0/16',target:'local'},{id:'private',destination:'10.0.0.0/8',target:'tgw-123'},{id:'default',destination:'0.0.0.0/0',target:'igw-123'}];
export function RoutingSimulator(){ const [dest,setDest]=useState('8.8.8.8'); const result=useMemo(()=>lookupRoute(routes,dest),[dest]); return <section className="card p-5"><h2 className="text-2xl font-black">Module 10 — Route Table Simulator</h2><p className="text-slate-400">Longest prefix match and route priority explain every forwarding decision.</p><input className="input mt-4 max-w-md" value={dest} onChange={e=>setDest(e.target.value)}/><div className="mt-4 grid gap-4 lg:grid-cols-2"><div className="rounded-xl border border-white/10"><table className="w-full text-sm"><thead><tr className="bg-white/10"><th className="p-2 text-left">Destination</th><th className="p-2 text-left">Target</th></tr></thead><tbody>{routes.map(r=><tr className={`border-t border-white/10 ${result.route?.id===r.id?'bg-cyanx/10':''}`} key={r.id}><td className="p-2 font-mono">{r.destination}</td><td className="p-2 font-mono">{r.target}</td></tr>)}</tbody></table></div><div className="card p-4"><h3 className="font-bold">Lookup Result</h3><p className="mt-2 text-cyan-100">{result.reason}</p><p className="mt-2 text-sm text-slate-400">Candidates: {result.candidates.map(c=>c.destination).join(', ') || 'none'}</p></div></div></section> }
