import { useSelectedProject } from '../../application/ipamStore';
export function IpamPanel(){ const p=useSelectedProject(); return <section className="card p-5"><h2 className="text-2xl font-black">Module 8 — Local-first IPAM</h2><p className="text-slate-400">Projects, organizations, VPCs, networks, host inventory, allocations, reservations, locks, duplicate detection and reports are persisted in browser storage.</p><div className="mt-4 grid gap-4 md:grid-cols-3"><Metric label="VPCs" value={p.vpcs.length}/><Metric label="Subnets" value={p.vpcs.flatMap(v=>v.subnets).length}/><Metric label="Hosts" value={p.hosts.length}/></div><pre className="mt-4 overflow-auto rounded-xl bg-black/30 p-4 text-xs">{JSON.stringify(p,null,2)}</pre></section>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-2xl border border-white/10 bg-white/10 p-4"><div className="text-sm text-slate-400">{label}</div><div className="text-4xl font-black text-cyan-100">{value}</div></div>}
